/**
 * Calibration — React surface.
 *
 * `CalibrationTab` is the whole user-facing face of this domain; it lived in
 * `components/controls/tabs/` until 2026-09-05, which left the domain folder
 * looking like three loose files instead of a subsystem.
 */
export { default as CalibrationTab } from './controls/CalibrationTab';
export { EnvelopeWaveformPreview } from './EnvelopeWaveformPreview';
